<UserControl x:Class="Attendance.Wpf.Views.LoginView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Margin="0,0,0,8" x:Name="Username" PlaceholderText="Username"/>
        <PasswordBox Grid.Row="1" Margin="0,0,0,8" x:Name="Password"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
            <Button Content="Login" Width="100" Click="OnLogin"/>
        </StackPanel>
    </Grid>
</UserControl>
